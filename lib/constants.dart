import 'package:flutter/material.dart';

// Dark Mode Colors
const primaryColorDark = Color(0xFF1E75B6); // Darker blue for dark mode
const secondaryColorDark = Color(0xFF1F232E); // Dark secondary color
const bgColorDark = Color(0xFF121421); // Darker background
const warmColorDark = Color.fromARGB(255, 255, 48, 48); // Vivid red for dark mode

// Light Mode Colors
const primaryColorLight = Color(0xFF2697FF); // Original primary blue
const secondaryColorLight = Color(0xFFF5F5F5); // Light gray for secondary elements
const bgColorLight = Color(0xFFFFFFFF); // White background for light mode
const warmColorLight = Color.fromARGB(255, 255, 65, 65); // Bright red for light mode

const defaultPadding = 16.0;
